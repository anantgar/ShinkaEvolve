# Candidate contract

Implement `src/packing.py` with:

```python
run_packing() -> (centers, radii, reported_sum)
```

The result must contain exactly 41 circles in the unit square, with no
overlaps. `reported_sum` must equal the sum of the returned radii. The secure
evaluator recomputes and validates every value.
