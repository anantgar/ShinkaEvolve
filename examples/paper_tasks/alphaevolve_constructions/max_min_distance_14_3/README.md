# AlphaEvolve distance ratio `(14, 3)`

This isolated task minimizes the largest-to-smallest pairwise distance ratio
of 14 points in `R^3`. `seed_repo/solution.py` is mutable; `evaluate.py` is the
external exact numerical evaluator.
