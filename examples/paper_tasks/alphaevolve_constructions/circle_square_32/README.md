# AlphaEvolve variable-radius circle packing, 32 circles

The mutable seed constructs 32 circles; the external evaluator checks exact
unit-square containment and pairwise non-overlap, then sums their radii.

