# AlphaEvolve Hermite uncertainty construction

This isolated task evolves three Hermite coefficients. The external evaluator
derives the fourth constraint coefficient and uses the released verifier's
exact SymPy real-root and sign-change calculation for the published C4 bound.
