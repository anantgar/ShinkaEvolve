evo:
  seed_repo_path: seed_repo
  mutable_paths: []
  immutable_paths: []
  agent_hidden_paths: []
  language: python
  task_sys_msg: |
    Improve construct_matrix() in solution.py. Return a 29x29 matrix whose
    entries are exactly +1 or -1. Maximize its exact absolute determinant; the
    invisible evaluator uses fraction-free integer elimination and scores log(det).
job:
  eval_program_path: evaluate.py
